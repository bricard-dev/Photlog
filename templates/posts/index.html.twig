{% extends 'layouts/base.html.twig' %}

{% block title 'Home' %}

{% block body %}
    <div class="row">
        <div class="col-md-9">
            <div class="row">
            {% for col in posts|batch(3) %}
                <div class="col-md-6">
                    {% for post in col %}
                        {% include "posts/partials/_post.html.twig" %}
                    {% endfor %}
                </div>
            {% endfor %}
            </div>
        </div>
        <div class="col-md-3">

        </div>
    </div>
{% endblock %}
