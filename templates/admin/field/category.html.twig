{% for category in field.value %}
  {#{%- set url = ea_url()
    .setController('Controller\\Admin\\CategoryCrudController')
    .setAction('detail')
    .setEntityId(category.id)
  -%}#}
  {#<a href="{{ url }}">#}
    {{ category.name }}{% if not loop.last %}, {% endif %}
  {#</a>#}
{% else %}  
  <span class="badge badge-secondary">None</span>
{% endfor %}